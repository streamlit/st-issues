# Dependabot configuration
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates

version: 2
updates:
  # Direct Python dependency bumps. The weekly full lock refresh runs Monday.
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "wednesday"

    open-pull-requests-limit: 3

    cooldown:
      default-days: 5

    labels:
      - "dependencies"
      - "python"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "wednesday"

    cooldown:
      default-days: 5

    labels:
      - "dependencies"
      - "ci"
